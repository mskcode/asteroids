#pragma once

namespace game {

enum class ShaderProgramId {
    UNKNOWN = 0,
    DEFAULT,
    GLYPH,
};

enum class TextureId {
    UNKNOWN = 0,
    SMILEY,
};

enum class FontId {
    UNKNOWN = 0,
    ARCADE,
};

enum class FontBitmapCacheId {
    UNKNOWN = 0,
    ARCADE_48,
};

} // namespace game